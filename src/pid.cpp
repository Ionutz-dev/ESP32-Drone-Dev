#include "pid.h"

void initPID() {
    // TODO: Initialize PID controllers
}

PIDOutput computePID(float currentRoll, float currentPitch, float currentYaw, Commands cmd) {
    // TODO: Compute PID values based on orientation and commands
    PIDOutput pid = {0,0,0};
    return pid;
}
