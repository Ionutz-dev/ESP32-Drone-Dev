#include "receiver.h"

void initReceiver() {
    // TODO: Set up receiver input pins (PWM/iBus)
}

Commands readReceiver() {
    // TODO: Read remote signals (PWM/iBus)
    Commands cmd = {0,0,0,0};
    return cmd;
}
